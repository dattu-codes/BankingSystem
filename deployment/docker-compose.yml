version: '3.8'

services:
  banking-backend:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: banking-system-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
